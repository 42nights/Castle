/** Toolkit slugs are free-form strings; Composio's catalog has 1000+.
 *  Keep this loose so the connections page can render the whole set
 *  without us maintaining a curated list. */
export type ToolkitSlug = string;
